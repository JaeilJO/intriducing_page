import Link from 'next/link';
import { styled } from 'styled-components';

const S = {
    Main: styled.main`
        display: flex;
        flex-direction: column;
        gap: 50px;
    `,
};
export default S;
